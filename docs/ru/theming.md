# Темизация

Не каждый бот говорит по-английски. Не каждому дизайну нужны одинаковые иконки. Система темизации teleflow позволяет заменить каждую метку кнопки, индикатор состояния и сообщение об ошибке.

## Установка темы

Передайте `UITheme` в `TGApp`, и она применится ко всему:

```python
from teleflow.uilib.theme import UITheme, NavUI, SelectionUI, ActionUI, DisplayUI, ErrorUI

theme = UITheme(
    nav=NavUI(back="Назад", back_arrow="◀ Назад"),
    action=ActionUI(done="Готово ✓", cancel="Отменено."),
)

tg = TGApp(key_node=UserId, theme=theme)
```

Переопределять нужно только то, что хотите изменить — всё остальное сохраняет значения по умолчанию.

Отдельные паттерны тоже принимают параметр `theme`, если нужна кастомизация для конкретного паттерна.

## Компоненты темы

`UITheme` состоит из пяти вложенных датаклассов, каждый отвечает за категорию строк.

### NavUI — кнопки навигации

Управляет кнопками пагинации и навигации назад в browse, dashboard и flows.

| Поле | По умолчанию | Используется в |
|------|-------------|---------------|
| `prev` | `"◀"` | Стрелка страницы |
| `next` | `"▶"` | Стрелка страницы |
| `prev_label` | `"◀️ Prev"` | Полная кнопка «назад» |
| `next_label` | `"Next ▶️"` | Полная кнопка «вперёд» |
| `back` | `"Back"` | Текст кнопки «назад» |
| `back_arrow` | `"◀ Back"` | Кнопка «назад» со стрелкой |

### SelectionUI — индикаторы выбора

Управляет отображением выбранного/невыбранного состояния в Radio, Multiselect, Toggle и вкладках фильтров.

| Поле | По умолчанию | Используется в |
|------|-------------|---------------|
| `checked` | `"✅"` | Multiselect — выбрано |
| `unchecked` | `"⬜"` | Multiselect — не выбрано |
| `radio_on` | `"🔘"` | Radio — выбрано |
| `radio_off` | `"⚪"` | Radio — не выбрано |
| `toggle_on` | `"🟢"` | Toggle — вкл |
| `toggle_off` | `"🔴"` | Toggle — выкл |
| `tab_active` | `"🔘"` | Активная вкладка фильтра |
| `tab_inactive` | `"⚪"` | Неактивная вкладка фильтра |

### ActionUI — метки кнопок

Управляет текстом на кнопках действий во всех паттернах.

| Поле | По умолчанию | Используется в |
|------|-------------|---------------|
| `done` | `"Done ✓"` | Кнопка «готово»/подтверждения |
| `ok` | `"OK"` | Кнопка OK |
| `yes` | `"Yes"` | Да в диалоге подтверждения |
| `no` | `"No"` | Нет в диалоге подтверждения |
| `cancel` | `"Cancelled."` | Сообщение об отмене |
| `remove_last` | `"Remove last"` | ListBuilder |
| `decrement` | `"−"` | Counter — минус |
| `increment` | `"+"` | Counter — плюс |

### DisplayUI — форматирование значений

Управляет отображением значений в обзоре settings, сводках и пагинации.

| Поле | По умолчанию | Используется в |
|------|-------------|---------------|
| `none_value` | `"(not set)"` | Незаданные поля |
| `bool_true` | `"Yes"` | Boolean true |
| `bool_false` | `"No"` | Boolean false |
| `no_options` | `"(no options available)"` | Пустые динамические опции |
| `entity_not_found` | `"Entity not found."` | Отсутствующая сущность в browse |
| `disabled_date` | `"·"` | Недоступная дата в DatePicker |
| `date_format` | `"%b %d, %Y"` | Формат даты |
| `page_format` | `"{}/{}"` | Счётчик страниц (напр., «1/5») |

### ErrorUI — ошибки и валидация

Каждое сообщение об ошибке, которое может увидеть пользователь, настраивается. Форматные строки используют `{}` для динамических значений.

| Поле | По умолчанию | Используется в |
|------|-------------|---------------|
| `use_buttons` | `"Please use the buttons above."` | Неверный тип ввода (несколько кнопок) |
| `use_button` | `"Please use the button above."` | Неверный тип ввода (одна кнопка) |
| `send_text` | `"Please send a text message."` | TextInput |
| `send_photo` | `"Please send a photo."` | PhotoInput |
| `send_document` | `"Please send a document."` | DocumentInput |
| `send_video` | `"Please send a video."` | VideoInput |
| `send_voice` | `"Please send a voice message."` | VoiceInput |
| `send_location` | `"Please share a location."` | LocationInput |
| `send_contact` | `"Please use the Share Contact button."` | ContactInput |
| `send_number` | `"Please enter a number."` | NumberInput |
| `send_media` | `"Please send a photo, document, or video."` | MediaGroupInput |
| `use_calendar` | `"Please use the calendar buttons above."` | DatePicker |
| `use_time_picker` | `"Please use the time picker buttons above."` | TimePicker |
| `use_slider` | `"Please use the slider buttons above."` | Slider |
| `enter_pin` | `"Please enter all digits first."` | PinInput |
| `select_option` | `"Please select an option first."` | Radio/Inline |
| `select_rating` | `"Please select a rating first."` | Rating |
| `select_days` | `"Please select at least one day."` | RecurrencePicker |
| `too_short` | `"Too short (min {} chars)"` | Валидация MinLen |
| `too_long` | `"Too long (max {} chars)"` | Валидация MaxLen |
| `invalid_format` | `"Invalid format (expected {})"` | Валидация Pattern |
| `range_error` | `"Must be between {} and {}."` | Диапазон чисел |
| `min_select` | `"Select at least {}"` | Минимум Multiselect |
| `max_items` | `"Max {} items"` | Максимум ListBuilder/MediaGroup |
| `max_reached` | `"Maximum {} items reached. Press Done."` | Достижение лимита ListBuilder |
| `min_required` | `"Please add at least {} items."` | Минимум ListBuilder/MediaGroup |

## Пример: русская тема

Полная локализация — навигация, действия, метки отображения и частые ошибки:

```python
ru_theme = UITheme(
    nav=NavUI(
        prev_label="◀️ Назад",
        next_label="Вперед ▶️",
        back="Назад",
        back_arrow="◀ Назад",
    ),
    action=ActionUI(
        done="Готово ✓",
        ok="ОК",
        yes="Да",
        no="Нет",
        cancel="Отменено.",
        remove_last="Удалить последний",
        decrement="−",
        increment="+",
    ),
    display=DisplayUI(
        none_value="(не задано)",
        bool_true="Да",
        bool_false="Нет",
        no_options="(нет вариантов)",
        date_format="%d.%m.%Y",
        entity_not_found="Не найдено.",
    ),
    errors=ErrorUI(
        use_buttons="Используйте кнопки выше.",
        use_button="Используйте кнопку выше.",
        send_text="Отправьте текстовое сообщение.",
        send_photo="Отправьте фотографию.",
        send_number="Введите число.",
        too_short="Слишком коротко (мин. {} символов)",
        too_long="Слишком длинно (макс. {} символов)",
        select_option="Сначала выберите вариант.",
    ),
)

tg = TGApp(key_node=UserId, theme=ru_theme)
```

---

**Назад: [Трансформы](transforms.md)**

[Оглавление](readme.md)
