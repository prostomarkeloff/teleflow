# Темизация

Каждая строка и иконка в teleflow настраивается через `UITheme`. Передайте кастомную тему в `TGApp` или в отдельные паттерны.

## UITheme

```python
from teleflow.uilib.theme import UITheme, NavUI, SelectionUI, ActionUI, DisplayUI, ErrorUI

theme = UITheme(
    nav=NavUI(...),
    selection=SelectionUI(...),
    action=ActionUI(...),
    display=DisplayUI(...),
    errors=ErrorUI(...),
)

tg = TGApp(key_node=UserId, theme=theme)
```

## Компоненты

### NavUI — кнопки навигации

| Поле | По умолчанию | Описание |
|------|-------------|----------|
| `prev` | `"◀"` | Стрелка «предыдущая страница» |
| `next` | `"▶"` | Стрелка «следующая страница» |
| `prev_label` | `"◀️ Prev"` | Метка кнопки «назад» |
| `next_label` | `"Next ▶️"` | Метка кнопки «вперёд» |
| `back` | `"Back"` | Текст кнопки «назад» |
| `back_arrow` | `"← Back"` | Кнопка «назад» со стрелкой |

### SelectionUI — индикаторы выбора

| Поле | По умолчанию | Описание |
|------|-------------|----------|
| `checked` | `"✅"` | Выбранный элемент (multiselect) |
| `unchecked` | `"⬜"` | Невыбранный элемент (multiselect) |
| `radio_on` | `"🔘"` | Выбранный вариант (radio) |
| `radio_off` | `"⭕"` | Невыбранный вариант (radio) |
| `toggle_on` | `"🟢"` | Переключатель вкл |
| `toggle_off` | `"🔴"` | Переключатель выкл |
| `tab_active` | индикатор | Активная вкладка фильтра |
| `tab_inactive` | индикатор | Неактивная вкладка фильтра |

### ActionUI — метки кнопок

| Поле | По умолчанию | Описание |
|------|-------------|----------|
| `done` | `"Done"` | Кнопка «готово» |
| `ok` | `"OK"` | Кнопка OK |
| `yes` | `"Yes"` | Да (диалог подтверждения) |
| `no` | `"No"` | Нет (диалог подтверждения) |
| `cancel` | `"Cancelled."` | Сообщение об отмене |
| `remove_last` | `"Remove last"` | Удалить последний (ListBuilder) |
| `decrement` | `"−"` | Декремент (Counter) |
| `increment` | `"+"` | Инкремент (Counter) |

### DisplayUI — форматирование

| Поле | По умолчанию | Описание |
|------|-------------|----------|
| `none_value` | `"(not set)"` | Отображение None |
| `bool_true` | `"Yes"` | Boolean true |
| `bool_false` | `"No"` | Boolean false |
| `no_options` | `"(no options available)"` | Пустые динамические опции |
| `date_format` | `"%b %d, %Y"` | Формат даты |
| `page_format` | `"{}/{}"` | Формат счётчика страниц |

### ErrorUI — ошибки и валидация

| Поле | По умолчанию | Описание |
|------|-------------|----------|
| `use_buttons` | `"Please use the button(s) above"` | Неверный тип ввода |
| `send_text` | `"Please send a text message"` | Ожидается текст |
| `send_photo` | `"Please send a photo"` | Ожидается фото |
| `send_document` | `"Please send a document"` | Ожидается документ |
| `too_short` | ошибка min length | Валидация MinLen |
| `too_long` | ошибка max length | Валидация MaxLen |
| `select_option` | `"Please select an option"` | Вариант не выбран |
| `min_select` | ошибка min selection | Минимум multiselect |
| `max_items` | ошибка max items | Максимум ListBuilder/MediaGroup |

## Пример: русская тема

```python
ru_theme = UITheme(
    nav=NavUI(
        prev_label="◀️ Назад",
        next_label="Вперед ▶️",
        back="Назад",
        back_arrow="← Назад",
    ),
    action=ActionUI(
        done="Готово",
        ok="ОК",
        yes="Да",
        no="Нет",
        cancel="Отменено.",
    ),
    display=DisplayUI(
        none_value="(не задано)",
        bool_true="Да",
        bool_false="Нет",
        no_options="(нет вариантов)",
        date_format="%d.%m.%Y",
    ),
    errors=ErrorUI(
        use_buttons="Используйте кнопки выше",
        send_text="Отправьте текстовое сообщение",
    ),
)
```
